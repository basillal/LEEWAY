{
  "version": 2,
  "builds": [
    { "src": "shell/**", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/remoteEntry.js", "dest": "/remoteEntry.js" },
    { "src": "/assets/(.*)", "dest": "/assets/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}